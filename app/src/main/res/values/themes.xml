<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.CoinSpirit2" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color -->
        <item name="colorPrimary">@color/blue</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Background color -->
        <item name="android:colorBackground">@color/white</item>
        <item name="colorOnBackground">@color/black</item>

        <!-- Accent color for elements -->
        <item name="colorAccent">@color/item_color</item>

        <!-- Icon color -->
        <item name="iconColor">@color/black</item> <!-- Цвет иконок в светлой теме -->
    </style>
    <style name="Theme.CoinSpirit2" parent="Base.Theme.CoinSpirit2" />
</resources>